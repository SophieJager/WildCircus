{% extends 'base.html.twig' %}

{% block title %}PricesDay{% endblock %}

{% block body %}
    <h1>PricesDay</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ prices_day.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ prices_day.name }}</td>
            </tr>
            <tr>
                <th>Active</th>
                <td>{{ prices_day.active }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('prices_day_index') }}">back to list</a>

    <a href="{{ path('prices_day_edit', {'id': prices_day.id}) }}">edit</a>

    {{ include('prices_day/_delete_form.html.twig') }}
{% endblock %}
