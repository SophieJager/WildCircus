<form method="post" action="{{ path('prices_day_delete', {'id': prices_day.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ prices_day.id) }}">
    <button class="btn">Delete</button>
</form>
