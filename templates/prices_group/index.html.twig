{% extends 'base.html.twig' %}

{% block title %}PricesGroup index{% endblock %}

{% block body %}
    <h1>PricesGroup index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Active</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for prices_group in prices_groups %}
            <tr>
                <td>{{ prices_group.id }}</td>
                <td>{{ prices_group.name }}</td>
                <td>{{ prices_group.active }}</td>
                <td>
                    <a href="{{ path('prices_group_show', {'id': prices_group.id}) }}">show</a>
                    <a href="{{ path('prices_group_edit', {'id': prices_group.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('prices_group_new') }}">Create new</a>
{% endblock %}
