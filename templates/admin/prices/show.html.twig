{% extends 'admin/base.html.twig' %}

{% block title %}Prices{% endblock %}

{% block body %}
    <h1>Prices</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ price.id }}</td>
            </tr>
            <tr>
                <th>Day</th>
                <td>{{ price.day.name }}</td>
            </tr>
            <tr>
                <th>Group</th>
                <td>{{ price.groups.name }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ price.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('prices_index') }}">back to list</a>

    <a href="{{ path('prices_edit', {'id': price.id}) }}">edit</a>

    {{ include('admin/prices/_delete_form.html.twig') }}
{% endblock %}
