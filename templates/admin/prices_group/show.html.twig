{% extends 'admin/base.html.twig' %}

{% block title %}PricesGroup{% endblock %}

{% block body %}
    <h1>PricesGroup</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ prices_group.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ prices_group.name }}</td>
            </tr>
            <tr>
                <th>Active</th>
                <td>{{ prices_group.active }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('prices_group_index') }}">back to list</a>

    <a href="{{ path('prices_group_edit', {'id': prices_group.id}) }}">edit</a>

    {{ include('admin/prices_group/_delete_form.html.twig') }}
{% endblock %}
